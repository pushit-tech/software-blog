import { allPosts, Post } from 'contentlayer/generated'
import PostCard from '@/context/blog/components/post-card';

export default function PostCardList({ posts }) {
  return (
    <div>
      {
        // posts.map(<PostCard key={post.id} title={post.title} body={post.body} />)
        
      }
      hola
    </div>
  );
};
